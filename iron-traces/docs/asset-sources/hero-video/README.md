# 首页坦克驶近视频

模型：OpenRouter MiniMax H3 (`minimax/hailuo-3`)。
参数：15 秒，2K，16:9，无生成音频，首尾帧控制。
首帧：内置 imagegen 在原首页上将坦克移到道路远端；末帧：原 campaign-menu-no-progress.png。两帧保存在 public/images/campaign-approach-{first,last}.png。
提示词及参数：request.json。生成任务状态：job.json。

页面只显示视频头部，字标与关卡保留原始静态图。每次进入菜单播放一次，结束保留末帧；进入关卡或后台暂停。减少动态效果及视频加载失败时回退原图。

生成脚本从隐藏输入读取密钥，密钥不保存到项目。请求只在本地生成阶段使用，浏览器仅加载最终 MP4。

验收：视频已生成，2560×1440，15.084秒，8420430字节。浏览器验证静音自动播放及结束保留末帧通过，构建通过。供应商费用1.95美元。返回文件含AAC轨道，页面始终静音。
