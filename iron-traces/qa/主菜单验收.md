# 主菜单
采用确认生成图campaign-menu.png；等比例全屏适配，六个真实按钮覆盖关卡区域。悬停/聚焦抬升、点击压下、锁定抖动；支持减少动画系统偏好。程序合成短按键音与静音持久化（当前未接入MIDI配乐）。
浏览器验证：六卡片可见；锁定点击提示前关条件；首卡Enter可进入简报；开始后playing；暂停返回简报再回战役；静音刷新仍保持。菜单期间暂停底层3D绘制。其余五关尚未实现；首关通关后第二关状态为已解锁·制作中，重玩不累计解锁其他关。
